// usePermissions — Re-export of usePermission for backward compatibility.
// The canonical hook is usePermission (singular).
// Both names work identically.

export { usePermission as usePermissions } from './usePermission';
